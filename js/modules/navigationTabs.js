import { all, $ } from '../utils/helpers.js';
export function initNavigationTabs(ctx) { all('.tab-btn').forEach(button => button.addEventListener('click', () => { all('.tab-btn').forEach(item => item.classList.remove('active')); all('.tab-panel').forEach(panel => panel.classList.remove('active')); button.classList.add('active'); $(button.dataset.tab)?.classList.add('active'); })); all('.admin-tab-btn').forEach(button => button.addEventListener('click', async () => { all('.admin-tab-btn').forEach(item => item.classList.remove('active')); all('.admin-tab-panel').forEach(panel => { panel.classList.remove('active'); panel.style.display = 'none'; }); button.classList.add('active'); const panel = $(button.dataset.adminTab); if (panel) { panel.classList.add('active'); panel.style.display = 'block'; } if (button.dataset.adminTab === 'adminSavedGraphsPanel') await ctx.api.renderSavedGraphsTab(); })); }
