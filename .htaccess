<IfModule mod_rewrite.c>
    RewriteEngine On

    # Lets a standard XAMPP htdocs install open the Laravel app at /iris
    # while still keeping the real Laravel entry point inside /public.
    RewriteRule ^$ public/ [L]
    RewriteRule ^(.+)$ public/$1 [L]
</IfModule>
